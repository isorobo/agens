---
phase: 00-vault-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\Users\Simon\Documents\wiki-agents\30_Concepts\agent-patterns-index.md
  - C:\Users\Simon\Documents\wiki-agents\10_Sources\Books\agentic-design-patterns-gulli-2025.md
  - C:\Users\Simon\Documents\wiki-agents\40_Guides\agentic-design-patterns-plain-english.md
  - C:\Users\Simon\Documents\wiki-agents\10_Sources\Books\agentic-ai-handbook-bar-2025.md
  - C:\Users\Simon\Documents\wiki-agents\30_Concepts\workflow-vs-autonomous-agent.md
  - C:\Users\Simon\Documents\wiki-agents\30_Concepts\react.md
  - C:\Users\Simon\Documents\wiki-agents\10_Sources\Blog\anthropic-building-effective-agents.md
autonomous: true
requirements: [SETUP-01]

must_haves:
  truths:
    - "The MOC dataview query resolves to exactly one queryable note: agent-patterns-index.md."
    - "The index note names all 21 Gulli patterns as bold body text a citation check matches by pattern name."
    - "Each of the six source notes lost topic/agent-patterns and carries a forward pointer to the index."
    - "The plain-english guide retains at least one valid topic (topic/foundations) after the tag is removed, satisfying schema section 2.1."
    - "The date-header format is confirmed in CONTEXT.md (D-10/D-11/D-12) as agens-log's future format; Phase 0 writes no log file."
  artifacts:
    - path: "C:\\Users\\Simon\\Documents\\wiki-agents\\30_Concepts\\agent-patterns-index.md"
      provides: "Canonical 21-pattern lookup target, sole carrier of topic/agent-patterns"
      contains: "topic/agent-patterns"
      min_lines: 60
  key_links:
    - from: "50_MOCs/MOC - Agent Patterns.md dataview query"
      to: "30_Concepts/agent-patterns-index.md"
      via: "contains(topic, topic/agent-patterns) AND type != moc"
      pattern: "topic/agent-patterns"
    - from: "each of the six source notes"
      to: "30_Concepts/agent-patterns-index.md"
      via: "See also forward pointer wikilink"
      pattern: "agent-patterns-index"
---

<objective>
Collapse the wiki-agents vault's six-source topic/agent-patterns cluster into one canonical concept note, then narrow the tag so the existing MOC dataview query resolves to that single note.

Purpose: SETUP-01 requires later phases to ground recommendations against one lookup target, not six scattered sources. Phase 1's citation-resolves check (RECOMMEND-05) reads this note for pattern names as body text.

Output:
- One new index note at 30_Concepts/agent-patterns-index.md carrying all 21 Gulli patterns.
- Six source notes with topic/agent-patterns removed and a forward pointer added.
- Deterministic grep verification that the single-lookup-target invariant holds.

This phase writes ZERO code. It edits Obsidian markdown in an external vault at C:\Users\Simon\Documents\wiki-agents, outside this git repo. There is no build, no test suite, no package manager. Acceptance criteria are grep and file-existence checks against the vault.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-vault-consolidation/00-CONTEXT.md
@.planning/phases/00-vault-consolidation/00-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Vault access note: the vault is an external directory outside this repo. Confirm the path is readable and writable. If a sandbox blocks it, grant it with --add-dir "C:/Users/Simon/Documents/wiki-agents".

Verification hygiene note: the vault holds a .smart-env/multi/*.ajson embeddings cache and 99_Meta/ vocabulary docs that also contain the literal string topic/agent-patterns. Every verification grep MUST restrict to --include=*.md and exclude .smart-env/ and 99_Meta/, or the counts read false positives. This was confirmed at planning time.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical index note with all 21 patterns</name>
  <files>C:\Users\Simon\Documents\wiki-agents\30_Concepts\agent-patterns-index.md</files>
  <read_first>
    - C:\Users\Simon\Documents\wiki-agents\30_Concepts\best-practices-index.md (structural precedent: frontmatter, entry shape, section order)
    - C:\Users\Simon\Documents\wiki-agents\30_Concepts\anti-patterns-index.md (second identical precedent)
    - C:\Users\Simon\Documents\wiki-agents\40_Guides\agentic-design-patterns-plain-english.md (source trigger/trade-off text for all 21 patterns; lines 46-148 name the patterns, lines 162-177 model the trigger/trade-off pair)
    - C:\Users\Simon\Documents\wiki-agents\10_Sources\Books\agentic-design-patterns-gulli-2025.md (provenance spine for defined_in)
    - C:\Users\Simon\Documents\wiki-agents\10_Sources\Blog\anthropic-building-effective-agents.md (crosswalk terms: Evaluator-Optimiser, Orchestrator-Workers)
    - C:\Users\Simon\Documents\wiki-agents\99_Meta\schema.md (frontmatter field rules section 2; linking rule section 3 requires at least three related concepts)
    - .planning/phases/00-vault-consolidation/00-RESEARCH.md (the 21-pattern table, exact frontmatter template, entry-format template, Gulli-Anthropic crosswalk)
  </read_first>
  <action>
    Create the new file 30_Concepts/agent-patterns-index.md with the Write tool (this is a new file, not an edit). Copy the frontmatter and section skeleton from best-practices-index.md verbatim, then fill the values RESEARCH.md specifies.

    Frontmatter fields and values (per RESEARCH.md "Exact frontmatter schema", D-01, D-04): set type to concept; status to draft; created to 2026-07-12; name to Agent Patterns Index; slug to agent-patterns-index; the topic list to the single value topic/agent-patterns (this note becomes the SOLE carrier after Task 2, per D-08); tags to index and design-patterns; synonyms to agent design patterns and agentic patterns; defined_in to the Gulli book wikilink [[10_Sources/Books/agentic-design-patterns-gulli-2025|Agentic Design Patterns (Gulli)]] (D-04 provenance spine); related_concepts to at least three wikilinks, using [[react]], [[workflow-vs-autonomous-agent]], and [[the-agent-loop]] (satisfies schema section 3 minimum); anthropic to false (the spine is Gulli, not Anthropic primary material).

    Body sections in this order (per RESEARCH.md "Exact body section skeleton"): a title heading Agent Patterns Index; a one-line blockquote summary; a Summary paragraph stating this note consolidates the six-source topic/agent-patterns cluster into one lookup target with Gulli's 21 patterns as the spine; a Patterns section holding all 21 entries; an optional Detail section for the four-part grouping; a Related list of the three-plus wikilinks; a Sources list linking the Gulli book and the plain-english guide; a See also list linking [[MOC - Agent Patterns]].

    Write all 21 entries in the Patterns section using these exact bold names in this order (D-05, verbatim from RESEARCH.md's 21-pattern table): Prompt Chaining; Routing; Parallelisation; Reflection; Tool Use; Planning; Multi-Agent Collaboration; Memory Management; Learning and Adaptation; Model Context Protocol (MCP); Goal Setting and Monitoring; Exception Handling and Recovery; Human-in-the-Loop; Knowledge Retrieval (RAG); Inter-Agent Communication (A2A); Resource-Aware Optimisation; Reasoning Techniques; Guardrails and Safety Patterns; Evaluation and Monitoring; Prioritisation; Exploration and Discovery.

    Each entry follows D-03: the bold pattern name, a one-clause statement of what it is, an explicit Trigger clause (the symptom that says use this now), and an explicit Trade-off clause (the cost it buys against). Label the two clauses with the literal words Trigger: and Trade-off: so the verification grep matches them. Condense the source text from the plain-english guide rather than re-derive it (Don't Hand-Roll rule). Write no bare one-liners; each entry must give Phase 1 an independently quotable trigger-plus-trade-off passage.

    Apply the D-07 crosswalk inline on the two patterns whose Anthropic name differs: on Reflection, note that Anthropic names the generator-plus-evaluator flavour Evaluator-Optimiser; on Multi-Agent Collaboration, note that Anthropic names the lead-delegates-to-workers coordination Orchestrator-Workers. For Prompt Chaining, Routing, and Parallelisation, note the name matches Anthropic. Create no separate entries per source terminology.

    Apply D-04 link-outs on exactly two entries: on Planning (entry 6), add a See [[react]] for depth clause because ReAct is its interleaved-reasoning strategy; on Resource-Aware Optimisation (entry 16), add a See [[workflow-vs-autonomous-agent]] for depth clause because it carries the agents-versus-workflows distinction. No other entry dead-ends into a second file.

    Confine the write to the vault path above. Create no per-pattern stub notes (D-02 forbids this). Do not use the type value pattern (D-02 keeps it unused). Write only the 21 named Gulli patterns; do not read or draw from the Bar handbook's BDI/SOAR/ACT-R material (D-06 excludes it as academic cognitive architecture, not an actionable implementation pattern).
  </action>
  <verify>
    <automated>cd "C:/Users/Simon/Documents/wiki-agents" && test -f "30_Concepts/agent-patterns-index.md" && for p in "Prompt Chaining" "Routing" "Parallelisation" "Reflection" "Tool Use" "Planning" "Multi-Agent Collaboration" "Memory Management" "Learning and Adaptation" "Model Context Protocol (MCP)" "Goal Setting and Monitoring" "Exception Handling and Recovery" "Human-in-the-Loop" "Knowledge Retrieval (RAG)" "Inter-Agent Communication (A2A)" "Resource-Aware Optimisation" "Reasoning Techniques" "Guardrails and Safety Patterns" "Evaluation and Monitoring" "Prioritisation" "Exploration and Discovery"; do grep -qF "**$p**" "30_Concepts/agent-patterns-index.md" || echo "MISSING PATTERN: $p"; done; echo "trigger=$(grep -c 'Trigger:' '30_Concepts/agent-patterns-index.md') tradeoff=$(grep -c 'Trade-off:' '30_Concepts/agent-patterns-index.md')"</automated>
  </verify>
  <acceptance_criteria>
    - test -f "30_Concepts/agent-patterns-index.md" exits 0.
    - Each of the 21 bold pattern names above appears in the note as bold text; the verify loop prints no MISSING PATTERN line.
    - grep -c 'Trigger:' returns 21 (every entry states a trigger, per D-03).
    - grep -c 'Trade-off:' returns 21 (every entry states a trade-off, per D-03).
    - grep -qF 'type: concept' succeeds and grep -qF 'type: pattern' fails (D-02 keeps the pattern type unused).
    - The frontmatter topic list carries topic/agent-patterns.
    - grep -qF '[[react]]' and grep -qF '[[workflow-vs-autonomous-agent]]' both succeed (D-04 link-outs present).
    - grep -c '\[\[' returns at least 3 (schema section 3 related-concept minimum).
  </acceptance_criteria>
  <done>The index note exists, names all 21 patterns as bold text with a labelled trigger and trade-off each, carries the correct frontmatter with topic/agent-patterns, and links the two D-04 depth notes.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow the tag and add forward pointers across the six source notes</name>
  <files>C:\Users\Simon\Documents\wiki-agents\10_Sources\Books\agentic-design-patterns-gulli-2025.md, C:\Users\Simon\Documents\wiki-agents\40_Guides\agentic-design-patterns-plain-english.md, C:\Users\Simon\Documents\wiki-agents\10_Sources\Books\agentic-ai-handbook-bar-2025.md, C:\Users\Simon\Documents\wiki-agents\30_Concepts\workflow-vs-autonomous-agent.md, C:\Users\Simon\Documents\wiki-agents\30_Concepts\react.md, C:\Users\Simon\Documents\wiki-agents\10_Sources\Blog\anthropic-building-effective-agents.md</files>
  <read_first>
    - C:\Users\Simon\Documents\wiki-agents\10_Sources\Books\agentic-design-patterns-gulli-2025.md
    - C:\Users\Simon\Documents\wiki-agents\40_Guides\agentic-design-patterns-plain-english.md
    - C:\Users\Simon\Documents\wiki-agents\10_Sources\Books\agentic-ai-handbook-bar-2025.md
    - C:\Users\Simon\Documents\wiki-agents\30_Concepts\workflow-vs-autonomous-agent.md
    - C:\Users\Simon\Documents\wiki-agents\30_Concepts\react.md
    - C:\Users\Simon\Documents\wiki-agents\10_Sources\Blog\anthropic-building-effective-agents.md
    - C:\Users\Simon\Documents\wiki-agents\99_Meta\schema.md (section 2.1: every note needs at least one topic; section 3: no orphan notes)
    - .planning/phases/00-vault-consolidation/00-RESEARCH.md (The Definitive Six-File Edit List)
  </read_first>
  <action>
    Edit each of the six source notes below with the Edit tool (never Write; these are existing human-authored notes and overwriting them destroys content). Two changes per file, per D-08 (narrowing the tag so the index becomes its sole carrier): remove the topic/agent-patterns list item from the frontmatter topic block, and add a forward pointer to the index.

    Depends on Task 1: the index note must already exist so the [[agent-patterns-index]] wikilinks resolve (D-09).

    For each file, remove ONLY the single topic/agent-patterns list line from the topic block; keep every other topic tag intact. Confirmed remaining tags (verified at planning time): gulli-2025 keeps topic/architectures; bar-2025 keeps topic/foundations; workflow-vs-autonomous-agent keeps topic/architectures; react keeps topic/architectures; anthropic-building-effective-agents keeps topic/foundations.

    Resolve the plain-english guide gap (RESEARCH.md Open Question 1; the guide's ONLY topic is topic/agent-patterns, so a bare removal leaves zero topics and violates schema section 2.1). In 40_Guides/agentic-design-patterns-plain-english.md, FIRST add a topic/foundations list line to the topic block, THEN remove the topic/agent-patterns line. The guide is a foundational map of the 21 patterns, so topic/foundations is the correct controlled replacement. Net result: the guide carries topic/foundations as its sole topic. Do not leave this note with an empty topic list.

    For each of the six files, add a one-line forward pointer under a See also section (D-09): a bullet reading [[agent-patterns-index]] followed by an em dash and the words consolidated pattern reference. If a file already has a See also section, append the line to it; if not, add the section near the end of the note, matching the vault's existing section placement.

    Do NOT touch these files (RESEARCH.md do-not-touch list): 50_MOCs/MOC - Agent Patterns.md (carries the tag but is type moc, excluded from its own query, and stays the navigation hub); 99_Meta/schema.md and 99_Meta/design/2026-07-10-wiki-agents-foundation-design.md (mention the string only as controlled-vocabulary documentation). Confine every write to the six vault paths above.
  </action>
  <verify>
    <automated>cd "C:/Users/Simon/Documents/wiki-agents" && for f in "10_Sources/Books/agentic-design-patterns-gulli-2025.md" "40_Guides/agentic-design-patterns-plain-english.md" "10_Sources/Books/agentic-ai-handbook-bar-2025.md" "30_Concepts/workflow-vs-autonomous-agent.md" "30_Concepts/react.md" "10_Sources/Blog/anthropic-building-effective-agents.md"; do echo "$f tag=$(grep -c 'topic/agent-patterns' "$f") ptr=$(grep -c 'agent-patterns-index' "$f") topics=$(grep -c '^- topic/' "$f")"; done</automated>
  </verify>
  <acceptance_criteria>
    - For each of the six files, grep -c 'topic/agent-patterns' returns 0 (tag removed).
    - For each of the six files, grep -c 'agent-patterns-index' returns at least 1 (forward pointer present, D-09).
    - For each of the six files, grep -c '^- topic/' returns at least 1 (schema section 2.1: no note left topicless).
    - grep -qF 'topic/foundations' on 40_Guides/agentic-design-patterns-plain-english.md succeeds and grep -c 'topic/agent-patterns' on it returns 0 (Open Question 1 resolved).
    - 50_MOCs/MOC - Agent Patterns.md still contains topic/agent-patterns (untouched, verified in Task 3).
  </acceptance_criteria>
  <done>All six source notes lost the topic/agent-patterns tag, each carries a forward pointer to the index, each retains at least one valid topic, and the plain-english guide now carries topic/foundations.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the single-lookup-target invariant and confirm the log-format record</name>
  <files>C:\Users\Simon\Documents\wiki-agents (read-only verification; no files written)</files>
  <read_first>
    - C:\Users\Simon\Documents\wiki-agents\50_MOCs\MOC - Agent Patterns.md (the dataview query that must resolve to one note)
    - .planning/phases/00-vault-consolidation/00-CONTEXT.md (D-10, D-11, D-12: the log-format record satisfying success criterion 3)
    - .planning/phases/00-vault-consolidation/00-RESEARCH.md (Verification Approach table)
  </read_first>
  <action>
    Run the deterministic verification below. Write no files. Restrict every grep to markdown with --include=*.md and exclude the .smart-env cache and the 99_Meta vocabulary docs, or the embeddings cache and schema docs read as false positives (confirmed at planning time).

    Check 1 (single lookup target): count markdown files carrying topic/agent-patterns, excluding the cache and meta docs. The queryable set is exactly two files: the new index (30_Concepts/agent-patterns-index.md) and 50_MOCs/MOC - Agent Patterns.md. The MOC is excluded from its own dataview query by the type != moc clause, so the query itself resolves to exactly one note: the index. This is the literal single lookup target the phase goal states.

    Check 2 (log-format confirmation, success criterion 3): confirm CONTEXT.md records the date-header decision (D-10, D-11, D-12). Phase 0 writes no log file; the record alone satisfies the criterion. Confirm no Phase 0 task created or edited any recent.md or 99_Meta/agens-log.md file (Pitfall 1: both would be wrong for Phase 0).

    If any check fails, do not mark the plan complete: a non-zero tag count on a source note means Task 2 missed a file; a queryable count other than two means a tag was missed or the index tag was omitted (Pitfall 2).
  </action>
  <verify>
    <automated>cd "C:/Users/Simon/Documents/wiki-agents" && echo "=== queryable carriers (expect exactly 2: index + MOC) ===" && grep -rl --include='*.md' 'topic/agent-patterns' . | grep -v -e '/.smart-env/' -e '/99_Meta/' && echo "=== count ===" && grep -rl --include='*.md' 'topic/agent-patterns' . | grep -v -e '/.smart-env/' -e '/99_Meta/' | wc -l && echo "=== MOC still tagged (expect 1) ===" && grep -c 'topic/agent-patterns' "50_MOCs/MOC - Agent Patterns.md"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rl --include='*.md' 'topic/agent-patterns' . excluding /.smart-env/ and /99_Meta/ lists exactly two files: 30_Concepts/agent-patterns-index.md and 50_MOCs/MOC - Agent Patterns.md, and the wc -l count is 2.
    - grep -c 'topic/agent-patterns' on 50_MOCs/MOC - Agent Patterns.md returns 1 (MOC untouched).
    - grep -qF 'D-10' .planning/phases/00-vault-consolidation/00-CONTEXT.md succeeds and CONTEXT.md records the date-header format for the future 99_Meta/agens-log.md (success criterion 3 met by record, per D-12).
    - No file named recent.md or agens-log.md was created or edited by this phase (Pitfall 1 avoided).
  </acceptance_criteria>
  <done>The dataview query resolves to exactly one note (the index), the MOC is untouched, no source note retains the tag, and CONTEXT.md's log-format record confirms success criterion 3 without any log file write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor process to external vault filesystem | The executor writes markdown into a directory outside the git repo. Writes must stay inside the vault. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | File writes to the vault | mitigate | Confine every write to the seven vault paths in files_modified (security.md section 7 path confinement). No path is derived from user input. |
| T-00-02 | Tampering | MOC, schema.md, and design doc | mitigate | The edit list is restricted to the six named source files. Explicit do-not-touch list protects the vocabulary docs and the MOC from a tag strip that would break navigation. |
| T-00-03 | Denial of service / data loss | Existing human-authored source notes | mitigate | Task 2 uses Edit for single-line changes, never Write-overwrite. Only the new index note uses Write. |
| T-00-SC | Tampering | Package installs | accept | No package manager, no installs, no external dependency in this phase. Supply-chain vector does not apply. |
</threat_model>

<verification>
Phase-level checks, all deterministic and Obsidian-free:

1. Index exists: test -f 30_Concepts/agent-patterns-index.md.
2. All 21 patterns named as bold body text (Task 1 verify loop prints no MISSING PATTERN).
3. Six sources lost the tag: grep -c 'topic/agent-patterns' returns 0 in each.
4. Six forward pointers present: grep -c 'agent-patterns-index' returns at least 1 in each.
5. No source note left topicless: grep -c '^- topic/' returns at least 1 in each; the plain-english guide carries topic/foundations.
6. Single lookup target: markdown files carrying the tag, excluding .smart-env and 99_Meta, count exactly two (index plus MOC); the MOC is excluded from the query by type != moc, so the query resolves to one note.
7. Success criterion 3: CONTEXT.md records the date-header decision; no log file was written.
</verification>

<success_criteria>
- One canonical concept note (30_Concepts/agent-patterns-index.md) collapses the six topic/agent-patterns sources into a single lookup target the MOC query resolves to.
- The note names each of the 21 patterns as bold body text a later citation check matches by pattern name.
- The date-header format is confirmed in CONTEXT.md (D-10/D-11/D-12) as agens-log's future format; no Phase 0 log file is written.
- Every source note retains at least one valid topic; the plain-english guide carries topic/foundations.
</success_criteria>

<output>
Create .planning/phases/00-vault-consolidation/00-01-SUMMARY.md when done.
</output>
