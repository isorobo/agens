---
phase: 01-agens-read-only-recommend
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: []
autonomous: false
requirements: [RECOMMEND-01, RECOMMEND-02]
must_haves:
  truths:
    - "User confirms /agens loads on the explicit command"
    - "User confirms agens auto-triggers on unseen project-starting phrasings and does not fire on the should-not-trigger set"
    - "User confirms the four-question call, the citation-grounded recommendation, and the plain refusal all behave"
  artifacts: []
  key_links:
    - from: "trigger-tests.md phrasing oracle"
      to: "live agens auto-trigger behaviour"
      via: "human runs each should-trigger and should-not-trigger phrasing in a fresh conversation"
      pattern: "should-trigger"
---

<objective>
Verify the agens read-only recommend spine works end to end in a live session — the one part of this phase that requires a human. Auto-trigger firing (RECOMMEND-02) cannot be proven by a grep; it needs a person to type unseen project-starting phrasings in fresh conversations and observe whether agens loads. This checkpoint runs the `trigger-tests.md` oracle against the live skill and walks the questionnaire, recommendation, and refusal paths.

Purpose: RECOMMEND-01 and RECOMMEND-02 are invocation-surface behaviours the skill loader owns; the only checkable evidence is a human invoking the skill and observing the result.
Output: human confirmation (or a defect list routing to gap closure).
</objective>

<phase_goal>
**As a** developer, **I want to** confirm agens loads by command and by natural phrasing and returns a grounded recommendation or a plain refusal, **so that** I trust the spine before Phase 2 builds delegation on top of it.

Note: this user story is planner-derived from ROADMAP Phase 1 success criteria 1 and 3. Run `/gsd mvp-phase 1` to formalise the ROADMAP `**Goal:**` line if the canonical form is wanted.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-agens-read-only-recommend/01-RESEARCH.md
@.claude/skills/agens/SKILL.md
@.claude/skills/agens/references/trigger-tests.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of dual invocation, questionnaire, recommendation, and refusal</name>
  <files></files>
  <read_first>
    - .claude/skills/agens/references/trigger-tests.md (the should-trigger, should-not-trigger, and walkthrough oracle authored in Plan 01)
    - .claude/skills/agens/SKILL.md (the skill under test)
    - .planning/phases/01-agens-read-only-recommend/01-RESEARCH.md (Pattern 1 verification note; Pitfall 1 warning signs of auto-trigger failure)
  </read_first>
  <action>
    Pause for the user to run the six live checks below against the installed agens skill. This is a human-only verification: auto-trigger firing cannot be proven by an automated command, so the user drives the session while the executor records pass/fail per check and captures any failing phrasing for gap closure. Do not mark the task done until the user returns the resume signal.
  </action>
  <what-built>
    A single Claude Code Skill at `.claude/skills/agens/`: dual invocation (/agens and auto-trigger via the description), a single four-question AskUserQuestion call, a Read+Grep citation gate over `30_Concepts/agent-patterns-index.md`, a one-pattern recommendation with a quoted Trigger/Trade-off passage, and a plain refusal for non-matches. The agent-authored tagging convention is defined for Phase 4 (Plan 02).
  </what-built>
  <how-to-verify>
    Ensure the vault is granted for the session: run agens in a directory where the wiki-agents vault is available via `claude --add-dir "C:/Users/Simon/Documents/wiki-agents"` (or `/add-dir` in-session). `permissions.additionalDirectories` does NOT load skills — use --add-dir.
    1. Explicit command (RECOMMEND-01): type `/agens`. Confirm the skill loads and asks the four questions.
    2. Auto-trigger (RECOMMEND-02): in a FRESH conversation, type each "should-trigger" phrasing from `references/trigger-tests.md` one at a time. Confirm agens auto-loads on each without the /agens command.
    3. Over-firing guard: in a fresh conversation, type each "should-not-trigger" phrasing. Confirm agens does NOT load on any of them.
    4. Questionnaire (RECOMMEND-03): confirm all four questions (Goal, Workflow, Sensitivity, Latency) appear in ONE call, with the fixed option labels, before any recommendation.
    5. Recommendation (RECOMMEND-04/05): answer with a clearly-matching project (for example: answer-over-data / fixed workflow / internal / interactive). Confirm agens returns exactly one pattern, cites the vault-relative path `30_Concepts/agent-patterns-index.md`, and quotes the matched Trigger and Trade-off sentences.
    6. Refusal (RECOMMEND-06): answer with a shape no bold entry supports, or select the free-text "Other" on a dimension. Confirm agens refuses plainly, names the four searched dimensions, and cites nothing.
    If auto-trigger fires on a should-not-trigger phrasing or misses a should-trigger phrasing, note the exact phrasing so the description can be tuned (routes to gap closure).
  </how-to-verify>
  <verify>
    <human-check>User runs the six checks above and returns the resume signal; no automated command can prove auto-trigger firing.</human-check>
  </verify>
  <acceptance_criteria>
    - /agens loads the skill and reaches the four-question call.
    - Every should-trigger phrasing auto-loads agens; no should-not-trigger phrasing does.
    - The four questions appear in one call with the fixed labels before any recommendation.
    - A matching project yields one pattern, the vault-relative path, and the quoted Trigger/Trade-off passage.
    - A non-matching project or a free-text "Other" answer yields the plain refusal with no citation.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all six checks pass, or list the phrasings/steps that failed for gap closure.</resume-signal>
  <done>All six live checks pass and the user returns "approved", or failures are captured with exact phrasings for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user phrasing → skill loader | The loader decides whether agens fires; only a live human test proves the boundary behaves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (trigger scope) | Auto-trigger description | mitigate | Run the independent should-trigger / should-not-trigger oracle against the live skill; tune the description on any over- or under-fire before phase exit |
| T-03-02 | Spoofing (of grounding) | Live recommendation | mitigate | Human confirms the recommendation cites a resolvable path and a real quoted passage, not model recall |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — markdown only |
</threat_model>

<verification>
- All six live checks pass, or failures are captured with exact phrasings/steps for gap closure.
- Monolith-drift standing check: confirm agens stayed a router and grounding layer — it cited the vault note rather than reimplementing the pattern taxonomy inline.
</verification>

<success_criteria>
- RECOMMEND-01 and RECOMMEND-02 confirmed live: both invocation paths work and auto-trigger fires on unseen phrasings without over-firing.
- The questionnaire, recommendation, and refusal paths confirmed behaving in a live session.
</success_criteria>

<output>
Create `.planning/phases/01-agens-read-only-recommend/01-03-SUMMARY.md` when done.
</output>
