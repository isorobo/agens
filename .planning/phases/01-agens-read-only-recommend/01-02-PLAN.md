---
phase: 01-agens-read-only-recommend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agens/references/agent-authored-convention.md
autonomous: true
requirements: [RECOMMEND-07]
must_haves:
  truths:
    - "The agent-authored tagging convention is defined as a concrete, greppable marker for Phase 4 (agens-log, LOG-01) to enforce"
    - "The convention records that agens writes nothing to the vault in Phase 1"
  artifacts:
    - path: ".claude/skills/agens/references/agent-authored-convention.md"
      provides: "The agent-authored frontmatter marker, the in-note attribution line, and the verification grep Phase 4 will run"
      contains: "agent-authored"
      min_lines: 20
  key_links:
    - from: ".claude/skills/agens/references/agent-authored-convention.md"
      to: "wiki-agents/99_Meta (Phase 4 agens-log.md)"
      via: "the marker Phase 4 stamps on every note agens writes"
      pattern: "agent-authored"
---

<objective>
Define the agent-authored tagging convention that distinguishes any note agens writes from the user's own writing. Phase 1 writes nothing to the vault; this plan fixes the convention as a concrete, greppable marker so Phase 4's agens-log (LOG-01) enforces it rather than inventing one under time pressure.

Purpose: RECOMMEND-07 reads as a conditional guard — "IF agens writes a note, THEN it is tagged agent-authored." The antecedent is false in Phase 1, so the requirement holds vacuously; the real deliverable is the convention Phase 4 depends on.
Output: `.claude/skills/agens/references/agent-authored-convention.md`.
</objective>

<phase_goal>
**As a** vault owner, **I want to** distinguish notes agens writes from notes I write myself, **so that** the attribution boundary between agent-authored and human-authored knowledge holds as agens gains write capability.

Note: this user story is planner-derived from ROADMAP Phase 1 success criterion 5. Run `/gsd mvp-phase 1` to formalise the ROADMAP `**Goal:**` line if the canonical form is wanted.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-agens-read-only-recommend/01-RESEARCH.md
@.planning/phases/01-agens-read-only-recommend/01-CONTEXT.md

# The vault schema — confirms no agent-authorship field exists today:
@C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the agent-authored tagging convention for Phase 4</name>
  <files>.claude/skills/agens/references/agent-authored-convention.md</files>
  <read_first>
    - C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md (the vault's controlled frontmatter fields; confirm no agent-authorship field exists — the `anthropic:` boolean marks source material, not agent authorship)
    - .planning/phases/01-agens-read-only-recommend/01-RESEARCH.md (RECOMMEND-07 Scope Resolution; Assumptions A1 and A2)
    - .planning/phases/01-agens-read-only-recommend/01-CONTEXT.md (RECOMMEND-07 write-scope note; the user's standing preference for existing vault convention over invented structure)
    - .planning/STATE.md (Phase 4 decision on the vault's `_memory/recent.md` `## YYYY-MM-DD` date-header convention for agens-log)
  </read_first>
  <action>
    Create `.claude/skills/agens/references/agent-authored-convention.md` defining, as documentation only, how any note agens later writes is marked agent-authored. Choose a marker that fits the vault schema (schema.md §2) rather than inventing a competing structure, consistent with the user's standing preference. Specify three concrete elements:
    (1) a frontmatter marker — a new boolean or enum field (for example `authored_by: agens`) that does not collide with an existing schema.md field, chosen because schema.md defines no agent-authorship field today and the `anthropic:` boolean marks source material only;
    (2) an explicit in-note attribution line agens adds to the body of any note it writes, so the marker is visible without parsing frontmatter;
    (3) the exact verification grep Phase 4 will run to confirm a written note carries the marker (an anchored grep of the frontmatter field), so LOG-01 has a checkable enforcement, not a prose intention.
    State plainly at the top of the file that Phase 1 writes NOTHING to the vault — this file defines the convention only; the first and only vault write (`99_Meta/agens-log.md`) happens in Phase 4 (LOG-01). Do not create, edit, or write any file under the wiki-agents vault in this plan.
  </action>
  <verify>
    <automated>cd "C:/Users/Simon/claude-projects/agens" && test -f .claude/skills/agens/references/agent-authored-convention.md && grep -qi "agent-authored" .claude/skills/agens/references/agent-authored-convention.md && grep -Eqi "authored_by|frontmatter" .claude/skills/agens/references/agent-authored-convention.md && grep -qi "Phase 4" .claude/skills/agens/references/agent-authored-convention.md && grep -Eqi "writes nothing|no vault write|writes NOTHING" .claude/skills/agens/references/agent-authored-convention.md && test -z "$(find 'C:/Users/Simon/Documents/wiki-agents' -newermt '-10 minutes' -type f 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.claude/skills/agens/references/agent-authored-convention.md` exists.
    - It defines a frontmatter marker field that does not collide with an existing schema.md field.
    - It defines an explicit in-note attribution line for the body.
    - It states the exact anchored grep Phase 4 will run to enforce the marker.
    - It states that Phase 1 writes nothing to the vault and that the first vault write is Phase 4's `99_Meta/agens-log.md`.
    - No file under `C:/Users/Simon/Documents/wiki-agents` was created or modified by this plan.
  </acceptance_criteria>
  <done>The agent-authored marker, the in-note line, and the Phase 4 enforcement grep are defined; no vault write occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| convention doc → Phase 4 write | The marker defined here governs how future agens-authored notes are attributed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Repudiation | Attribution boundary | mitigate | Define a concrete, greppable agent-authored marker plus an in-note line so Phase 4 writes are distinguishable from human-authored notes (RECOMMEND-07) |
| T-02-02 | Elevation of privilege | Phase scope | mitigate | Convention is documentation only; Phase 1 tool grant excludes Write/Edit/Bash and this plan performs no vault write (verified by the freshness check) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — markdown only (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `.claude/skills/agens/references/agent-authored-convention.md` exists with the marker, the in-note line, the enforcement grep, and the no-write statement.
- No file under the wiki-agents vault changed during this plan.
</verification>

<success_criteria>
- RECOMMEND-07: the agent-authored tagging convention is defined for Phase 4 to enforce; Phase 1 writes nothing, so success criterion 5 holds vacuously.
</success_criteria>

<output>
Create `.planning/phases/01-agens-read-only-recommend/01-02-SUMMARY.md` when done.
</output>
