---
phase: 04-agens-log-append-only-logging
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - .claude/skills/agens/SKILL.md
autonomous: true
requirements: [LOG-01, LOG-02]
must_haves:
  truths:
    - "The append is an inline scoped Bash `>>` step in SKILL.md — the log is never read-modify-rewritten, so no prior entry can be overwritten"
    - "agens invokes the inline logging tail only on the Step 3 recommendation-success branch, after the recommendation is shown"
    - "The first write creates 99_Meta/agens-log.md with authored_by: agens frontmatter and the attribution line; every later write is a pure `>>` append of a day header (if new) and one entry line"
    - "Each entry stamps the cited note with a 12-char SHA-256, places at most one ## YYYY-MM-DD header per day via a bounded tail read, and verifies the anchored authored_by marker"
    - "Answer fields are stripped of embedded newlines and `## ` tokens before the entry line is composed, so a forged date header cannot be injected"
    - "A direct conversational request to write, edit, or delete the log returns the fixed D-03 refusal and triggers no shell call"
    - "The framework-fit delegation path logs nothing, preserving the D-09 tail rule"
    - "SKILL.md states the log is an output sink, never a grounding source, and the bounded tail read is a file-position check"
    - "allowed-tools carries the Wave-0-confirmed scoped Bash grant token(s) — one or several, matching the set 04-01 recorded for the composite tail — never a bare Bash or Bash(*)"
  artifacts:
    - path: ".claude/skills/agens/SKILL.md"
      provides: "Scoped Bash grant, the inline Step 3.5 logging tail (init/header/stamp/entry/verify/sanitise), the D-03 refusal block, and five Phase 4 anti-patterns"
      contains: "authored_by"
  key_links:
    - from: ".claude/skills/agens/SKILL.md Step 3.5 logging tail"
      to: "C:/Users/Simon/Documents/wiki-agents/99_Meta/agens-log.md"
      via: "inline scoped-Bash `>>` append on the Step 3 success branch"
      pattern: ">>"
    - from: ".claude/skills/agens/SKILL.md allowed-tools"
      to: "the inline append command"
      via: "Wave-0-confirmed scoped Bash(...) grant"
      pattern: "Bash\\("
---

<objective>
Wire the append into agens' recommendation flow per D-01/D-02: extend `allowed-tools` with the Wave-0-confirmed scoped Bash grant, add the inline `>>` logging tail on the Step 3 success branch only, script the D-03 refusal for direct log manipulation, and extend the anti-patterns.

Purpose: LOG-01 and LOG-02 become live behaviour. The append is an inline scoped Bash step inside SKILL.md — no separate skill, no bundled script (D-01) — with `>>` redirection as the mechanical no-overwrite guarantee (D-02). LOG-02 holds structurally (no command, no auto-trigger, no `Skill` target) and is reinforced by the D-03 fixed refusal against conversational and prompt-injected write requests.
Output: an edited SKILL.md that logs exactly one successful recommendation per turn via an inline append and refuses every other write path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agens-log-append-only-logging/04-CONTEXT.md
@.planning/phases/04-agens-log-append-only-logging/04-RESEARCH.md
@.planning/phases/04-agens-log-append-only-logging/04-PATTERNS.md
@.planning/phases/04-agens-log-append-only-logging/04-01-SUMMARY.md

<interfaces>
The mechanism decision delivered by Plan 04-01 (read its SUMMARY first):
  - PRIMARY (expected): inline `>>` append matches a scoped Bash grant. Wire the inline tail below with the confirmed grant token(s) — one, or the several 04-01 recorded for the composite sequence. Author no script.
  - FALLBACK (only if 04-01 recorded it): the inline `>>` append did not match a scoped grant. Take the explicit fallback branch in Task 1 — author `scripts/agens-log-append.sh`, grant `Bash(<resolving-path-variable>/scripts/agens-log-append.sh *)`, and have the SKILL.md tail call the script instead of inlining the commands.

The inline logging tail composes four sub-steps, verified in RESEARCH Code Examples (lines 265-287):
  - init-on-first-write: create 99_Meta/agens-log.md with `type: meta`, `status: permanent`, `created`, `topic: - topic/meta`, `authored_by: agens` frontmatter + title + attribution line
  - bounded tail read: `tail -n 50 "$LOG" | grep -E '^## [0-9]{4}-[0-9]{2}-[0-9]{2}' | tail -n1`; append `## <today>` only when the day changed
  - stamp: `sha256sum "$VAULT_ROOT/30_Concepts/agent-patterns-index.md" | cut -c1-12`
  - verify: anchored `grep -Eq '^authored_by:[[:space:]]*agens[[:space:]]*$' "$LOG"`; on non-zero, print LOG_WRITE_FAILED
  - the cited path for the current index is 30_Concepts/agent-patterns-index.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grant the scoped Bash write and add the inline logging tail</name>
  <files>.claude/skills/agens/SKILL.md</files>
  <read_first>
    - .claude/skills/agens/SKILL.md (line 14 allowed-tools; lines 30-39 vault-root resolution; Step 3 recommendation branch lines 165-191; lines 347-356 the unscopeable-Skill-grant anti-pattern for the least-privilege register)
    - .planning/phases/04-agens-log-append-only-logging/04-01-SUMMARY.md (the mechanism decision and the exact confirmed grant token)
    - .planning/phases/04-agens-log-append-only-logging/04-RESEARCH.md (Code Examples lines 265-287; grant forms lines 289-298; System Architecture Diagram lines 100-131; Patterns 1-3; Pitfalls 1-4; Anti-Patterns lines 196-202; Security Domain input-validation rows lines 372, 383)
    - .planning/phases/04-agens-log-append-only-logging/04-PATTERNS.md (Edit 1 and Edit 2, lines 73-169)
  </read_first>
  <action>
    Two coordinated edits to SKILL.md, driven by the 04-01 mechanism decision.

    Size pre-estimate first (Warning from the checker): SKILL.md is currently 356 lines. This task adds the grant line (~1), the Step 3.5 inline tail with its Bash block and guard prose (~35-45), and Task 2 adds the D-03 refusal (~10) and five anti-patterns (~10) — an estimated ~420-430 lines total, under the 500-line budget. Confirm the running count stays under 500 as you edit; if it approaches the cap, the reference-file variant was rejected (04-DISCUSSION-LOG Q1) — instead tighten prose, do not extract to a script (that is the fallback, gated by 04-01).

    First, extend the `allowed-tools` line (currently `allowed-tools: Read Grep Glob AskUserQuestion Skill`) with the exact scoped Bash token — or the full set of scoped tokens — that 04-01 confirmed. The composite logging tail is a five/six-command sequence (init test, bounded `tail`/`grep` read, `sha256sum` stamp, entry `printf`, anchored `grep` verify, `date`); 04-01's probe recorded whether the permission matcher covers this with ONE scoped token or requires SEPARATE tokens per command. Add exactly the set 04-01 recorded — one `Bash(...)` specifier or several. Use the documented prefix-plus-glob form for each, not the `Bash(git:*)` colon analogy from CLAUDE.md (RESEARCH Pitfall 2). Never widen to a bare `Bash` or `Bash(*)`; the tight token set is the write-surface control LOG-02 depends on.

    Second, insert a new `## Step 3.5: Log the recommendation` section immediately after the Step 3 recommendation branch and before `## Delegate a framework-fit question` (around current line 192). On the PRIMARY path, inline the four-step append as scoped Bash commands (RESEARCH Code Examples lines 265-287): init-on-first-write of the schema-conformant frontmatter and attribution line; the bounded-tail day-header placement; the SHA-256 stamp of the cited index note; the single entry-line `>>` append; and the anchored `authored_by` verify. Validate and sanitise every free-form field before it reaches the entry line — strip carriage returns and newlines and collapse each of the four questionnaire answers to one physical line (log-injection / forged-`## `-header guard, RESEARCH Security Domain), and reject a `cited_rel_path` containing `..` or a leading `/` (path-traversal guard). Every runtime write is `>>` or the single first-write `>`; never a read-modify-rewrite of existing content.

    Specify that this tail runs ONLY on the Step 3 recommendation-success branch — never after the plain refusal, and never on the delegation path (the D-09 tail rule forbids any agens output after the delegated skill; RESEARCH Anti-Patterns line 200). Resolve the vault root exactly as the Step 2b read path does (the resolution rule at lines 30-39); hard-code no absolute vault root; an unresolved root fails closed to no log write, never a guessed path. State plainly that logging is best-effort after delivery: a `LOG_WRITE_FAILED` token or a non-zero exit emits a fixed one-line log-failure notice and never retracts or blocks the already-shown recommendation. State explicitly that the log is an output sink, never a grounding source, and that the bounded tail read is a file-position check, not a semantic read of prior entries (ROADMAP criterion 3).

    FALLBACK branch (only if 04-01 recorded the inline grant did not match): instead of inlining the commands, author `.claude/skills/agens/scripts/agens-log-append.sh` holding the identical four-step logic behind `set -euo pipefail` and the argument signature `<vault_root> <pattern> <cited_rel_path> <goal> <workflow> <sensitivity> <latency>`, grant `Bash(<resolving-path-variable>/scripts/agens-log-append.sh *)` per the 04-01 result, and have the Step 3.5 tail call the script. Add the fallback script to this plan's files_modified in the SUMMARY if this branch is taken.
  </action>
  <verify>
    <automated>bash -c 'set -e; T="${TMPDIR:-/tmp}/agens-append-smoke"; rm -rf "$T"; mkdir -p "$T/99_Meta" "$T/30_Concepts"; printf "index bytes\n" > "$T/30_Concepts/agent-patterns-index.md"; LOG="$T/99_Meta/agens-log.md"; run(){ TODAY="${3:-2026-07-15}"; if [ ! -f "$LOG" ]; then printf -- "---\ntype: meta\nstatus: permanent\ncreated: %s\ntopic:\n- topic/meta\nauthored_by: agens\n---\n\n# agens recommendation log\n\n> Agent-authored by agens.\n" "$TODAY" > "$LOG"; fi; LH="$(tail -n 50 "$LOG" | grep -E "^## [0-9]{4}-[0-9]{2}-[0-9]{2}" | tail -n1)"; [ "$LH" = "## $TODAY" ] || printf -- "\n## %s\n" "$TODAY" >> "$LOG"; S="$(sha256sum "$T/30_Concepts/agent-patterns-index.md" | cut -c1-12)"; P="$(printf "%s" "$1" | tr -d "\r\n")"; printf -- "- **%s** @ %s\n" "$P" "$S" >> "$LOG"; }; run "alpha"; F1="$(grep -c "alpha" "$LOG")"; run "beta"; run $'"'"'inj\n## 1999-01-01'"'"'; grep -Eq "^authored_by:[[:space:]]*agens[[:space:]]*$" "$LOG" || { echo MARKER_FAIL; exit 1; }; [ "$(grep -c "alpha" "$LOG")" = "1" ] || { echo OVERWRITE_FAIL; exit 1; }; [ "$(grep -c "^## 2026-07-15" "$LOG")" = "1" ] || { echo HEADER_FAIL; exit 1; }; grep -q "^## 1999-01-01" "$LOG" && { echo INJECTION_FAIL; exit 1; }; echo SMOKE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `allowed-tools` contains the Wave-0-confirmed `Bash(...)` specifier — one token, or the several scoped tokens 04-01 recorded for the composite sequence; it contains no bare `Bash` token and no `Bash(*)`.
    - A `## Step 3.5` logging section exists between Step 3 and `## Delegate a framework-fit question`.
    - PRIMARY path: the section inlines the `>>` append (init, bounded-tail header, sha256 stamp, entry line, anchored verify) and does NOT reference a bundled script. FALLBACK path only: it calls `scripts/agens-log-append.sh` and the script exists.
    - The section names the four passed answers, the cited path `30_Concepts/agent-patterns-index.md`, and the resolved vault root; it sanitises each answer (strips `\r`/`\n`) and rejects a `..`/leading-`/` cited path.
    - The section states: success-branch only; not on refusal; not on the delegation path (D-09); best-effort, never blocks the recommendation; log is never a grounding source.
    - The smoke test above exits 0 (`SMOKE_OK`), proving the append sequence gives first-write frontmatter, no-overwrite on repeat, one header per day, and injection sanitisation.
    - SKILL.md remains under 500 lines (`awk 'END{print NR}'` < 500).
  </acceptance_criteria>
  <done>agens logs one successful recommendation per turn through an inline scoped-Bash `>>` append (or, only if 04-01 required it, the fallback script), and never logs on the refusal or delegation paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add the D-03 fixed refusal and the five Phase 4 anti-patterns</name>
  <files>.claude/skills/agens/SKILL.md</files>
  <read_first>
    - .claude/skills/agens/SKILL.md (Step 3 refusal block lines 180-191 and Delegate part 4 failure message lines 311-324 — the fixed-refusal register; the `## Anti-patterns` section lines 326-356 — the bold-name-plus-sentence form)
    - .planning/phases/04-agens-log-append-only-logging/04-CONTEXT.md (D-03 wording constraints; Claude's Discretion on the refusal text)
    - .planning/phases/04-agens-log-append-only-logging/04-RESEARCH.md (Anti-Patterns lines 196-202; Security Domain threat table lines 376-383)
    - .planning/phases/04-agens-log-append-only-logging/04-PATTERNS.md (Edit 3 and Edit 4, lines 171-209)
  </read_first>
  <action>
    Two edits to SKILL.md.

    First, add a fixed D-03 refusal block for any direct conversational request to write, edit, or delete the log outside a recommendation flow. Follow the exact register of the two existing fixed blocks (Step 3 refusal, Delegate part 4): a fixed, quotable string that names what is closed and offers no partial substitute. The body states that logging runs only as the tail of a recommendation, is not a command, and agens will not append, edit, or delete entries on request. Wording is Claude's discretion within that register (CONTEXT.md). Place the block with the Step 3.5 logging section so the closed path sits beside the open one, and note that it triggers no shell call — it defends both the conversational trigger surface and the prompt-injection surface (a poisoned vault note attempting to force a write).

    Second, extend the `## Anti-patterns` section with five entries in the existing bold-name-plus-sentence form: (1) reading the log to inform a recommendation — the log is an output sink, Step 2 matching reads only the index note; (2) Write or Edit on the log at runtime — every runtime write is the inline `>>` append, never a read-modify-rewrite; (3) logging after a delegation — the D-09 tail rule forbids any agens output after the delegated skill; (4) granting unrestricted Bash — the grant stays scoped to the confirmed append token; (5) skipping the schema update — `authored_by` must be admitted to schema.md before first use.
  </action>
  <verify>
    <automated>grep -qiE 'not a command|will not (append|edit|delete)' .claude/skills/agens/SKILL.md && grep -c '^- \*\*' .claude/skills/agens/SKILL.md | awk '{print ($1>=11)?"ok":"few"}' | grep -q ok && awk 'END{print (NR<500)?"ok":"over"}' .claude/skills/agens/SKILL.md | grep -q ok</automated>
  </verify>
  <acceptance_criteria>
    - A fixed D-03 refusal block exists whose text states logging is not a command and that agens will not append, edit, or delete entries on request.
    - The block is a quotable fixed string in the same register as the Step 3 and Delegate part 4 blocks, and the surrounding text states it makes no shell call.
    - `## Anti-patterns` gains five new entries covering: reading the log as input, runtime Write/Edit on the log, logging after a delegation (D-09), unrestricted Bash, and skipping the schema update.
    - SKILL.md remains under 500 lines.
  </acceptance_criteria>
  <done>Every closed write path — direct request, prompt injection, delegation tail, log-as-input — has a fixed, scripted response, and the anti-patterns record the five failure modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md prose → shell command | agens' reasoning composes the argument string; the inline append treats every answer field as untrusted input |
| answer fields → log file bytes | questionnaire answers and the pattern name cross into a persisted file a human later reads |
| conversation / vault note → agens | untrusted text may request or inject a log write |
| agens reasoning → Bash grant | the grant is the only write mechanism; its scope is the write-surface control |
| delegation path → turn tail | D-09 forbids any agens output after the delegated skill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | inline `>>` append path | mitigate | `>>`-only at runtime plus a single first-write `>`; no read-modify-rewrite; the smoke test asserts the first entry survives a second same-day write |
| T-04-02 | Elevation of Privilege | allowed-tools Bash grant | mitigate | the Wave-0-confirmed scoped token(s) for the composite tail; never bare `Bash`/`Bash(*)`; acceptance criterion asserts the scoped form and 04-03 confirms no prompt fires live |
| T-04-03 | Tampering / Elevation | conversational or prompt-injected write request | mitigate | fixed D-03 refusal makes no shell call; the write runs only as the Step 3 success tail; the log is never read back as input, so a poisoned entry cannot re-enter reasoning |
| T-04-04 | Tampering | write target path | mitigate | the log path is the fixed vault-relative `99_Meta/agens-log.md`; reject `cited_rel_path` with `..` or a leading `/`; an unresolved vault root fails closed to no write |
| T-04-05 | Information Disclosure / Tampering | answer fields → entry line | mitigate | strip `\r`/`\n` and collapse each field to one physical line (blocks log injection and forged `## ` headers); the smoke test asserts no forged `## 1999-01-01` header appears |
| T-04-06 | Tampering | logging on the delegation path | mitigate | Step 3.5 runs on the recommendation-success branch only; anti-pattern 3 forbids any post-delegation write (D-09 tail rule) |
| T-04-07 | Repudiation | unresolved vault root | mitigate | fail closed — no log write on an unresolved root; never a guessed write path (reuses the Step 2b resolution rule) |
| T-04-INT | Repudiation | vault-state stamp | mitigate | SHA-256 content stamp of the cited note pins the exact bytes that grounded the recommendation (schema `wiki_hash` precedent) |
</threat_model>

<verification>
- The smoke test in Task 1 exits 0, proving inline no-overwrite, one-header-per-day, stamp, and injection sanitisation.
- `grep -Eq 'Bash\([^)]*'` matches the confirmed scoped token and no bare `Bash` token exists in `allowed-tools`.
- A `## Step 3.5` section exists; the D-03 refusal string is present; five new anti-patterns are present.
- SKILL.md line count is under 500.
</verification>

<success_criteria>
agens logs exactly one successful recommendation per turn via an inline scoped-Bash `>>` append (D-01/D-02 faithful), refuses every direct log-manipulation request with a fixed string, adds nothing on the delegation tail, and treats the log as an output sink. LOG-01 and LOG-02 are live and enforced by prose plus a real write-surface control. No bundled script exists unless 04-01 required the fallback.
</success_criteria>

<output>
Create `.planning/phases/04-agens-log-append-only-logging/04-02-SUMMARY.md` when done. Record: the final grant token shipped; whether the PRIMARY inline path or the FALLBACK script path was taken (and, if fallback, the script path added to files_modified); and the final SKILL.md line count, so Plan 03 deploys the matching artifacts.
</output>
