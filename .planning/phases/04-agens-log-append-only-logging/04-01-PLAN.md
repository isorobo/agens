---
phase: 04-agens-log-append-only-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md
autonomous: false
requirements: [LOG-01, LOG-02]
must_haves:
  truths:
    - "A Wave 0 empirical probe determines whether an inline `>>` append runs prompt-free under a scoped Bash grant, and records the exact working grant token"
    - "The probe resolves RESEARCH Open Question 3 — whether `${CLAUDE_SKILL_DIR}` expands inside allowed-tools — before any grant is committed"
    - "The write mechanism is decided before SKILL.md is wired: inline `>>` (primary, per D-01/D-02) or, only on probe failure, a bundled-script fallback"
    - "No script is authored on the primary path; the probe commits nothing to the skill directory"
    - "wiki-agents/99_Meta/schema.md admits authored_by as a controlled field before any real write"
  artifacts:
    - path: "C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md"
      provides: "authored_by controlled-vocabulary field, admitted before first use"
      contains: "authored_by"
  key_links:
    - from: "C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md"
      to: ".claude/skills/agens/references/agent-authored-convention.md"
      via: "the authored_by controlled value the log marker will carry"
      pattern: "authored_by"
---

<objective>
Decide the phase's write mechanism by empirical test before any commitment, and admit the agent-authorship field to the vault schema before first use.

Purpose: D-01 and D-02 lock the write mechanism to an inline scoped Bash append (`>>` redirection) added narrowly to agens' own `allowed-tools` — no separate skill, no bundled script. RESEARCH flags one version-sensitive unknown: the permission matcher's treatment of `>>` redirection is undocumented (Pitfall 1), and `${CLAUDE_SKILL_DIR}` resolution inside `allowed-tools` is unconfirmed (Open Question 3). This plan runs the Wave 0 probe that resolves both, so Plan 02 wires a grant known to match rather than one assumed to. The schema field is mechanism-independent and lands here so it precedes the first real write.
Output: a recorded mechanism decision (primary inline grant token, or fallback) and the `authored_by` schema field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agens-log-append-only-logging/04-CONTEXT.md
@.planning/phases/04-agens-log-append-only-logging/04-RESEARCH.md
@.planning/phases/04-agens-log-append-only-logging/04-PATTERNS.md
@.claude/skills/agens/references/agent-authored-convention.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave 0 empirical probe of the inline Bash write grant</name>
  <read_first>
    - .planning/phases/04-agens-log-append-only-logging/04-RESEARCH.md (Pitfall 1 redirection matching lines 232-236; Pitfall 2 colon-form lines 238-242; grant forms lines 289-298; Open Question 3 lines 344-347; State of the Art line 314)
    - .planning/phases/04-agens-log-append-only-logging/04-CONTEXT.md (D-01, D-02, and Claude's Discretion on the grant scoping syntax)
    - .claude/skills/agens/SKILL.md (line 14 allowed-tools; lines 347-356 the unscopeable-Skill-grant anti-pattern that governs least-privilege scoping)
  </read_first>
  <action>
    Automate the probe setup, then pause for the human to run it and report the observed behaviour. Do NOT commit anything to the agens skill directory: the probe is a spike, and D-01/D-02 forbid a bundled script on the primary path.

    Setup (automated): create a throwaway probe skill at `~/.claude/skills/agens-grant-probe/SKILL.md` (a scratch directory, deleted at the end of this task, never deployed and never referenced by agens). Give it two candidate `allowed-tools` grants to exercise the two open unknowns:
    1. Inline redirection: a scoped grant intended to authorise `printf '...' >> "$FILE"` — test the documented prefix-plus-glob form (for example `Bash(printf *)`), NOT the `Bash(git:*)` colon analogy from CLAUDE.md, which RESEARCH Pitfall 2 shows the current docs do not use.
    2. `${CLAUDE_SKILL_DIR}` resolution: a grant of the form `Bash(${CLAUDE_SKILL_DIR}/scripts/probe.sh *)` alongside a trivial `scripts/probe.sh` inside the probe skill only, to observe whether the variable expands inside `allowed-tools` (Open Question 3).
    Prepare the exact commands the human will run: one `printf '...' >> "$TMPFILE"` append against a temp file, and one invocation of the probe script through the `${CLAUDE_SKILL_DIR}` grant.

    Pause for the human to run the two commands in a live Claude Code session with the probe skill active, and to report — for each — whether it ran prompt-free (grant matched) or raised a permission prompt (grant did not match).
  </action>
  <what-built>
    A throwaway `agens-grant-probe` skill carrying two candidate Bash grants and two test commands: an inline `printf ... >> file` append, and a `${CLAUDE_SKILL_DIR}`-scoped script invocation. Nothing is written to the agens skill directory.
  </what-built>
  <how-to-verify>
    1. In a Claude Code session, confirm the `agens-grant-probe` skill is active (it appears in `/skills`).
    2. Run the prepared inline append command (`printf '...' >> "$TMPFILE"`). Record: did it run without a permission prompt, or did it prompt? Note the exact grant token in `allowed-tools`.
    3. Run the prepared `${CLAUDE_SKILL_DIR}/scripts/probe.sh` invocation. Record: did the `${CLAUDE_SKILL_DIR}` grant resolve and match (prompt-free), or did it prompt?
    4. Report the two outcomes and the exact grant token that matched (if any).
    Expected outcome — one of two decisions, recorded for Plan 02:
    - PRIMARY (inline works): the inline `>>` append ran prompt-free under a documented scoped token. Record that token; Plan 02 wires the inline logging tail with it. No script is authored.
    - FALLBACK (inline fails): the inline `>>` append prompted despite a scoped grant. Record whether the `${CLAUDE_SKILL_DIR}` script grant matched; Plan 02 takes the bundled-script fallback branch using the resolving path-variable form.
  </how-to-verify>
  <resume-signal>Report both outcomes and the matching grant token, then type "primary" (inline works) or "fallback" (author the script). Delete the probe skill before resuming.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Admit authored_by to the vault schema before first use</name>
  <files>C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md</files>
  <read_first>
    - C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md (§2.1-2.7 — the existing field-table format and the controlled-value-before-use rule)
    - .claude/skills/agens/references/agent-authored-convention.md (§1 — the field definition to enforce)
    - .planning/phases/04-agens-log-append-only-logging/04-RESEARCH.md (Schema field addition, lines 300-308)
  </read_first>
  <action>
    Add a new subsection `### 2.8 Agent-authorship marker` to `schema.md` §2, matching the exact three-column table shape the neighbouring subsections use (`Field | Enum values | Notes`). The single row defines `authored_by` with the one controlled value `agens` and the note that the field is present only on agent-written notes and absent on human-authored notes. Insert it after the last existing §2 subsection so the §2.7 material is not displaced. This edit is a human-convention update to a human-authored meta note: it must NOT itself carry `authored_by: agens` in schema.md's own frontmatter. Change nothing else in the file.
  </action>
  <verify>
    <automated>grep -Eq '^\|[[:space:]]*`?authored_by`?[[:space:]]*\|' "C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md" && grep -q '### 2.8' "C:/Users/Simon/Documents/wiki-agents/99_Meta/schema.md"</automated>
  </verify>
  <acceptance_criteria>
    - `schema.md` contains a `### 2.8` subsection with a table row defining `authored_by` and enum value `agens`.
    - The table uses the same `Field | Enum values | Notes` columns as the existing §2 subsections.
    - `schema.md`'s own frontmatter does NOT gain `authored_by: agens` (verify the frontmatter block is unchanged).
    - No existing §2.1-2.7 content is removed or reordered.
  </acceptance_criteria>
  <done>The vault schema admits authored_by before the deployed skill writes its first log entry, satisfying the schema's controlled-value-before-use rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agens reasoning → Bash grant | the grant is the only write mechanism; its scope is the write-surface control, and this plan proves it matches before it is committed |
| schema controlled vocabulary → vault content | the `authored_by` marker is valid vault content only after the schema admits it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Elevation of Privilege | allowed-tools Bash grant | mitigate | Wave 0 probe confirms the tightest documented scoped token matches the append; a bare `Bash`/`Bash(*)` is never used or tested — the probe validates least privilege before commit |
| T-04-09 | Tampering | undocumented `>>` grant matching | mitigate | the redirection-matching gap (RESEARCH Pitfall 1) is resolved empirically before Plan 02 relies on it; a non-matching grant routes to the documented script fallback rather than shipping a silently-broken append |
| T-04-10 | Repudiation | controlled-vocabulary governance | mitigate | schema.md admits `authored_by` before first use, per the vault's own controlled-value-before-use rule |
| T-04-SC | Tampering | package installs | accept | No npm/PyPI/crates install in this phase; RESEARCH Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- The Wave 0 probe outcome is recorded in the SUMMARY: inline-viable (with the exact grant token) or fallback, plus the `${CLAUDE_SKILL_DIR}`-in-allowed-tools result.
- The throwaway probe skill is deleted; nothing is committed to `.claude/skills/agens/`.
- `schema.md` §2.8 defines `authored_by`.
</verification>

<success_criteria>
The write mechanism is decided by test, not assumption: the SUMMARY records whether an inline `>>` append matches a scoped Bash grant (primary, D-01/D-02 faithful) or the bundled-script fallback is required, and whether `${CLAUDE_SKILL_DIR}` resolves in `allowed-tools`. The schema admits `authored_by` before any real write. No script is authored on the primary path.
</success_criteria>

<output>
Create `.planning/phases/04-agens-log-append-only-logging/04-01-SUMMARY.md` when done. Record, for Plan 02 to consume: (1) the mechanism decision — PRIMARY inline (with the exact working grant token) or FALLBACK bundled-script; (2) whether `${CLAUDE_SKILL_DIR}` resolved inside `allowed-tools`; (3) confirmation that the probe skill was deleted and nothing was committed to the agens skill directory.
</output>
