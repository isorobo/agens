---
phase: 02-delegation-wiring
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [DELEGATE-01, DELEGATE-02]

must_haves:
  truths:
    - "A live framework-fit opener routes to gsd-ai-integration-phase, not the pattern intake, and agens does not answer inline"
    - "A framework-fit opener outside an active GSD phase produces the fixed name+reason+next-step failure message"
    - "A non-framework opener still runs the Phase 1 pattern questionnaire unchanged"
  artifacts:
    - path: "~/.claude/skills/agens/SKILL.md"
      provides: "The deployed skill exercised in the live checkpoint"
      contains: "## Delegate a framework-fit question"
  key_links:
    - from: "user opening message"
      to: "gsd-ai-integration-phase"
      via: "Step 0 detection then Skill tool invocation"
      pattern: "gsd-ai-integration-phase"
    - from: "combined gate failure"
      to: "fixed failure message"
      via: "no inline answer"
      pattern: "rather than answering"
---

<objective>
Verify the delegation branch live: framework-fit routing, the fixed failure on a
missing precondition, and the unchanged Phase 1 pattern path.

Purpose: Confirms DELEGATE-01 and DELEGATE-02 hold in a real session before the phase
closes, and records the D-03 best-effort limitation as an observed fact.
Output: A human-confirmed verification of the three delegation behaviours.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-delegation-wiring/02-CONTEXT.md
@.planning/phases/02-delegation-wiring/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify live delegation routing, failure, and the unchanged pattern path</name>
  <files>~/.claude/skills/agens/SKILL.md (exercised, not modified)</files>
  <read_first>
    - .planning/phases/02-delegation-wiring/02-01-SUMMARY.md (what Task 02-01 authored and deployed)
    - .planning/phases/02-delegation-wiring/02-CONTEXT.md (D-03 best-effort limitation, D-08 initial-message scope, D-09 present-as-is)
    - ~/.claude/skills/agens/SKILL.md (the deployed skill under test)
  </read_first>
  <action>
    Present the four checks below to the user and pause for their observations. Modify
    no file: this is a verification-only checkpoint over the deployed skill. Run each
    opener in the stated context, observe agens' behaviour against the expected
    outcome, and record the D-03 re-ask behaviour in the SUMMARY. On any mismatch,
    capture what agens did versus what was expected and route the gap back to planning
    (gap closure) rather than editing the skill here.
  </action>
  <what-built>
    Task 02-01 added a framework-fit delegation branch to agens' SKILL.md — Step 0
    upfront trigger detection, a combined pre-flight gate (target-present Glob +
    active-GSD-phase read of STATE.md), a Skill-tool invocation of
    `gsd-ai-integration-phase` with the STATE.md phase integer, present-as-is
    hand-off, and a fixed name+reason+next-step failure message — and deployed the
    skill at user level (`~/.claude/skills/agens/`).
  </what-built>
  <how-to-verify>
    Run these four checks in a fresh session with agens loaded from the user-level
    install.

    1. Routing (DELEGATE-01), inside this GSD project: open with a framework-fit
       message such as "Which framework should I use for this — LangChain or the
       Claude Agent SDK?". Expected: agens skips the four-question pattern intake,
       runs the combined gate (both checks pass here — target present, active Phase 2),
       and invokes `gsd-ai-integration-phase` via the Skill tool with `2` as the
       phase argument. Expected NOT: agens answering the framework question from its
       own knowledge, or starting the pattern questionnaire.
    2. D-03 best-effort limitation: watch whether `gsd-ai-integration-phase` (or its
       sub-agents) re-asks goal / workflow / sensitivity / latency despite agens
       appending them as background. Record the observed behaviour. A re-ask is a
       known limitation, not a failure — the target reads phase CONTEXT.md, not the
       invocation text (RESEARCH Q2, Pitfall 2). Confirm agens presented the target's
       output as-is, with no paraphrase (D-09).
    3. Failure on missing precondition (DELEGATE-02): from a directory with no
       `.planning/STATE.md`, open with the same framework-fit message. Expected: the
       fixed failure message stating what is missing (no active GSD phase), why agens
       stops (it routes framework questions rather than answering them), and the next
       step (start or resume a GSD phase). Expected NOT: any inline framework opinion.
    4. Unchanged pattern path: open with a non-framework message such as "I want to
       build an assistant that answers questions over our internal docs." Expected:
       the Phase 1 four-question pattern intake runs unchanged; no delegation branch
       fires.
  </how-to-verify>
  <acceptance_criteria>
    - Check 1: the framework-fit opener triggers a Skill-tool call to `gsd-ai-integration-phase` with phase argument `2`; the four-question pattern intake does not run; agens gives no inline framework answer.
    - Check 2: the observed re-ask behaviour of the target is recorded; agens presented the target's output verbatim (no paraphrase or reframe).
    - Check 3: outside an active GSD phase, the framework-fit opener yields the fixed name+reason+next-step failure message and no inline answer.
    - Check 4: the non-framework opener runs the Phase 1 pattern questionnaire unchanged, with no delegation branch.
  </acceptance_criteria>
  <verify>
    <human-check>All four checks above observed and matching their expected outcomes; the D-03 re-ask behaviour recorded in the SUMMARY.</human-check>
  </verify>
  <done>All four checks observed against their expected outcomes, the D-03 re-ask behaviour recorded, and any mismatch routed to gap closure.</done>
  <resume-signal>Type "approved" if all four checks pass, or describe which check failed and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user conversation → agens | Live opening messages drive Step 0 detection during verification |
| local filesystem → agens | STATE.md presence and the phase integer gate the delegation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation | inline framework answer bypassing the router boundary | mitigate | Check 3 confirms a failed gate routes to the fixed refusal, never to an inline framework opinion (the core DELEGATE-02 failure mode) |
| T-02-07 | Tampering | wrong phase number passed to the target | mitigate | Check 1 confirms the STATE.md `Phase:` integer (`2`) is what reaches `$ARGUMENTS`, not a guessed value |
| T-02-SC | Tampering | package installs | accept | No package-manager install occurs this phase; verification only exercises existing files |
</threat_model>

<verification>
- Live routing, failure, and pattern-path behaviours match their expected outcomes across the four checks.
- The D-03 best-effort limitation is observed and recorded, not assumed.
</verification>

<success_criteria>
- DELEGATE-01 confirmed live: framework-fit routes to `gsd-ai-integration-phase` via the Skill tool, never answered inline.
- DELEGATE-02 confirmed live: a missing precondition yields the fixed failure message, never a silent inline reimplementation.
- Phase 1 pattern recommendation is untouched by the new branch.
</success_criteria>

<output>
Create `.planning/phases/02-delegation-wiring/02-02-SUMMARY.md` when done.
</output>
