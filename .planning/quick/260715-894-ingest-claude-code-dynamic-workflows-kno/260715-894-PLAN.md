---
phase: quick-260715-894
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "C:/Users/Simon/Documents/wiki-agents/10_Sources/Docs/claude-code-dynamic-workflows.md"
  - "C:/Users/Simon/Documents/wiki-agents/10_Sources/Blog/mindstudio-claude-code-5-workflow-patterns.md"
  - "C:/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md"
  - "README.md"
autonomous: true
requirements: [QUICK-260715-894]

must_haves:
  truths:
    - "Two vault source notes document Claude Code dynamic workflows and the MindStudio five-pattern taxonomy"
    - "The index note carries an additive Execution substrates section mapping the taxonomy onto its entries"
    - "Every existing bold entry, Trigger sentence, and Trade-off sentence in the index stays byte-identical"
    - "The index note remains the sole carrier of topic/agent-patterns across the vault"
    - "README Status table matches STATE.md and carries a Proposed execution-substrate subsection"
  artifacts:
    - path: "C:/Users/Simon/Documents/wiki-agents/10_Sources/Docs/claude-code-dynamic-workflows.md"
      provides: "Anthropic docs source note (source_type docs, anthropic true)"
      contains: "## Summary"
    - path: "C:/Users/Simon/Documents/wiki-agents/10_Sources/Blog/mindstudio-claude-code-5-workflow-patterns.md"
      provides: "MindStudio blog source note (source_type blog, anthropic false)"
      contains: "## Summary"
    - path: "C:/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md"
      provides: "Additive Execution substrates section plus two source wikilinks"
      contains: "## Execution substrates"
    - path: "README.md"
      provides: "Refreshed Status table and Proposed execution-substrate subsection"
      contains: "Proposed: execution-substrate"
  key_links:
    - from: "30_Concepts/agent-patterns-index.md Execution substrates table"
      to: "the two new source notes"
      via: "wikilinks appended under ## Sources"
      pattern: "claude-code-dynamic-workflows|mindstudio-claude-code"
---

<objective>
Ingest Claude Code dynamic-workflows knowledge into the wiki-agents vault, map the MindStudio five-pattern taxonomy onto the pattern index additively, and reference the change in README.md.

Purpose: agens may only recommend what the vault documents. The vault must gain the execution-substrate knowledge before any future agens change can cite it. This task lands the vault knowledge and records the proposal in the repo README.

Output:
- Two vault source notes (docs + blog) written from the provided reference material.
- An additive `## Execution substrates` section in the pattern index, plus two source wikilinks.
- A README Status-table refresh and a new Proposed subsection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260715-894-ingest-claude-code-dynamic-workflows-kno/260715-894-REFERENCE.md
@C:/Users/Simon/Documents/wiki-agents/90_Templates/template - source.md
@C:/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md
@README.md

<hard_constraints>
1. ADDITIVE ONLY on the index note. Insert `## Execution substrates` between `## Detail` and `## Related`. Append two wikilinks under `## Sources`. Every existing bold `**Name**` entry, every Trigger sentence, every Trade-off sentence, the `## Patterns` list, the frontmatter `topic` list, and the sole-carrier Summary claim stay byte-identical.
2. The two new source notes MUST NOT carry `topic/agent-patterns`. The index note stays the vault's sole carrier of that tag.
3. Source notes follow the vault template. Docs note: `anthropic: true`, `source_type: docs`. Blog note: `anthropic: false`, `source_type: blog`.
4. NO changes to `.claude/skills/agens/SKILL.md`. That follow-on is deferred.
5. Vault files live at `C:/Users/Simon/Documents/wiki-agents/` (outside the repo). Only `README.md` is committed. Vault changes are recorded in SUMMARY.md.
6. Write all note content from `260715-894-REFERENCE.md`, not model recall.
</hard_constraints>

<citation_gate_note>
agens Step 2b greps the index for bold `**Name**` literals and quotes Trigger/Trade-off sentences verbatim. The `## Execution substrates` section uses a markdown table, so its `**Prompt Chaining**` cells sit inside `|`-prefixed rows and never start a line with `- **`. This keeps the 21-count gate and the citation gate intact.
</citation_gate_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the two vault source notes</name>
  <files>
    C:/Users/Simon/Documents/wiki-agents/10_Sources/Docs/claude-code-dynamic-workflows.md
    C:/Users/Simon/Documents/wiki-agents/10_Sources/Blog/mindstudio-claude-code-5-workflow-patterns.md
  </files>
  <action>
Create both notes following `90_Templates/template - source.md` frontmatter and body order. Draw every fact from `260715-894-REFERENCE.md` (Source 1 for the docs note, Source 2 for the blog note). First grep the vault for the existing topic vocabulary (`grep -rho 'topic/[a-z-]*' "C:/Users/Simon/Documents/wiki-agents" | sort -u`) and pick one existing non-`topic/agent-patterns` tag that fits an execution-substrate note — prefer `topic/orchestration` if present, else fall back to the template default `topic/concepts`. Use the same tag on both notes. Never assign `topic/agent-patterns`.

Docs note frontmatter: `type: source`, `status: draft`, `created: 2026-07-15`, `title: Claude Code dynamic workflows`, `organisation: Anthropic`, `source_type: docs`, `anthropic: true`, `url: https://code.claude.com/docs/en/workflows`, `year: 2026`, `date_published:` blank, `topic:` the chosen tag, `tags: []`. Body: Summary (a dynamic workflow is a JavaScript script Claude writes that a runtime executes in the background while the session stays responsive); Key Concepts (who-holds-the-plan comparison across subagents / skills / agent teams / workflows — the script decides what runs next; "a workflow moves the plan into code"; scale of dozens to hundreds of agents, 16 concurrent, 1,000 cap; triggers `ultracode`, natural-language opt-in, `/effort ultracode`; save-for-reuse as `/commands`); Terminology (dynamic workflow, ultracode, acceptEdits mode); Best Practices (adversarial cross-review before reporting; run each sign-off stage as its own workflow; start with the bundled `/deep-research`); Warnings and Anti-Patterns (record the agens-relevant conflicts — no mid-run user input, so interactive intake and approval gates cannot run inside a workflow; workflows are an execution substrate, not a recommendation layer; token cost far above conversation with a Large-workflow warning past 25 agents or 1.5M projected tokens); Related Concepts (link `[[agent-patterns-index]]` and `[[workflow-vs-autonomous-agent]]`); References (the canonical URL).

Blog note frontmatter: `type: source`, `status: draft`, `created: 2026-07-15`, `title: Claude Code 5 workflow patterns explained`, `organisation: MindStudio`, `source_type: blog`, `anthropic: false`, `url: https://www.mindstudio.ai/blog/claude-code-5-workflow-patterns-explained`, `year: 2026`, `topic:` the same chosen tag, `tags: []`. Body: Summary (five structural approaches for organising Claude's agentic capabilities; pattern choice drives reliability, cost, and quality); Key Concepts (the five patterns — Sequential Flow, Operator, Split-and-Merge, Agent Teams, Headless — each with structure and when-to-use per Source 2); Terminology (define the five pattern names); Best Practices (start with simpler patterns before agent teams; validate a headless pattern in interactive mode first); Warnings and Anti-Patterns (agent teams are the most complex and hardest to debug from coordination overhead); Related Concepts (link `[[agent-patterns-index]]`); References (the canonical URL).

Apply NZ English spelling and Hemingway style in prose. Do not invent facts absent from the reference.
  </action>
  <verify>
    <automated>test -f "/c/Users/Simon/Documents/wiki-agents/10_Sources/Docs/claude-code-dynamic-workflows.md" && test -f "/c/Users/Simon/Documents/wiki-agents/10_Sources/Blog/mindstudio-claude-code-5-workflow-patterns.md" && grep -rl "topic/agent-patterns" "/c/Users/Simon/Documents/wiki-agents" --include="*.md" | wc -l</automated>
  </verify>
  <done>Both notes exist, follow the template body order, carry the correct anthropic/source_type flags, and the sole-carrier count for topic/agent-patterns still returns 1.</done>
</task>

<task type="auto">
  <name>Task 2: Add the additive Execution substrates section to the index note</name>
  <files>C:/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md</files>
  <action>
Before editing, save a pre-edit copy of the note to scratchpad for a regression diff: `cp "C:/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md" "$SCRATCH/index-before.md"` (use the session scratchpad path).

Insert a new section between the `## Detail` paragraph and the `## Related` heading. Do not touch any line in `## Patterns`, the frontmatter, or the Summary. The section reads:

## Execution substrates

Patterns are design-time choices. A substrate is where the pattern runs. Claude Code dynamic workflows are the native substrate when code holds the control.

| MindStudio pattern | Vault entry | Workflow-substrate fit |
|---|---|---|
| Sequential Flow | **Prompt Chaining** | Strong — script holds the step sequence |
| Operator | **Multi-Agent Collaboration** (Orchestrator-Workers) | Strong — script is the orchestrator |
| Split-and-Merge | **Parallelisation** | Strong — pipeline/parallel primitives |
| Agent Teams | **Multi-Agent Collaboration** | Partial — peer sessions suit agent teams; workflows suit fan-out |
| Headless | **Goal Setting and Monitoring** + autonomous loop | Conditional — validated-in-interactive-mode first |

A workflow cannot pause for user input, so Human-in-the-Loop and interactive intake stay outside the script.

Then append two wikilinks at the end of the existing `## Sources` list (after the Gulli and Plain-English entries), pointing to the two notes from Task 1:
- `[[10_Sources/Docs/claude-code-dynamic-workflows|Claude Code dynamic workflows]]`
- `[[10_Sources/Blog/mindstudio-claude-code-5-workflow-patterns|MindStudio — 5 workflow patterns]]`

After editing, diff the `## Patterns` block against the scratchpad copy to confirm zero change to existing entries.
  </action>
  <verify>
    <automated>grep -c "## Execution substrates" "/c/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md" && grep -c '^- \*\*' "/c/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md"</automated>
  </verify>
  <done>The section header count returns 1, the bold pattern-entry count still returns 21, both source wikilinks appear under Sources, and a diff of the Patterns block against the pre-edit copy shows no change.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh README Status table and add the Proposed subsection</name>
  <files>README.md</files>
  <action>
In the Status table (lines 46-51), change Phase 1 Status from `Not started` to `Complete` and Phase 2 Status from `Not started` to `In progress`. Leave Phase 0 (Complete), Phase 4 (Not started), and the Phase 3 deferral paragraph unchanged.

After the Phase 3 deferral paragraph and before `## Design principles`, insert this subsection verbatim (NZ English, Hemingway, no adverbs, sentences under 20 words, four sentences):

### Proposed: execution-substrate guidance

The vault now documents Claude Code dynamic workflows as a run-time substrate. The pattern index maps the MindStudio taxonomy onto its entries. A future change may surface a cited substrate line in agens recommendations. agens' own machinery stays interactive and never runs as a workflow.
  </action>
  <verify>
    <automated>grep -c "Proposed: execution-substrate" README.md</automated>
  </verify>
  <done>README Status shows Phase 1 Complete and Phase 2 In progress, the Proposed subsection renders after Status, and the grep returns 1.</done>
</task>

</tasks>

<verification>
Run from repo root (Git Bash):

- `test -f "/c/Users/Simon/Documents/wiki-agents/10_Sources/Docs/claude-code-dynamic-workflows.md"` — exit 0
- `test -f "/c/Users/Simon/Documents/wiki-agents/10_Sources/Blog/mindstudio-claude-code-5-workflow-patterns.md"` — exit 0
- `grep -c "## Execution substrates" "/c/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md"` — returns 1
- `grep -c '^- \*\*' "/c/Users/Simon/Documents/wiki-agents/30_Concepts/agent-patterns-index.md"` — returns 21
- `grep -rl "topic/agent-patterns" "/c/Users/Simon/Documents/wiki-agents" --include="*.md" | wc -l` — returns 1
- `grep -c "Proposed: execution-substrate" README.md` — returns 1

Manual regression: run `/agens` with Goal = "Orchestrate multiple specialists", Workflow = "Fixed workflow". The citation gate still passes and the recommendation quotes Multi-Agent Collaboration's Trigger/Trade-off verbatim.
</verification>

<success_criteria>
- Two vault source notes exist, follow the template, and carry correct anthropic/source_type flags.
- The index note has an additive Execution substrates section and two new source wikilinks, with every existing entry byte-identical.
- topic/agent-patterns remains carried by exactly one vault file.
- README Status table matches STATE.md and carries the Proposed subsection.
- SKILL.md is untouched.
- Commit covers README.md and the quick-task artifacts only; vault changes recorded in SUMMARY.md.
</success_criteria>

<output>
Create `.planning/quick/260715-894-ingest-claude-code-dynamic-workflows-kno/260715-894-SUMMARY.md` when done. Record the vault file changes there (they are outside git). Log the deferred SKILL.md follow-on (surface a cited execution-substrate line in agens' recommendation output) as a Pending Todo or via capture.
</output>
